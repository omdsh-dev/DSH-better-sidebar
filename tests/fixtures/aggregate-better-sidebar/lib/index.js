// No-op plugin body. This fixture only carries a patch layer (see
// cordis.patch.yml); no entry ever references this package by name, but the
// main file keeps npm pack's manifest valid.
export function apply() {}
